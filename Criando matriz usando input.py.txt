#Criar uma matrriz com random ou dizendo os números

import random
import numpy as np

matriz = []

# matriz = np.random.randint(0, 100, (5, 5))

for lin in range(2):
    linha = []
    for col in range(2):
        valor = int((input("Diga os números")))
        linha.append(valor)
    matriz.append(linha)
    
for lin in range(2):
    for col in range(2):
        print(f'{matriz[lin][col]:4}', end = ' ')
    print()